import { useParams, Link } from 'react-router-dom'
import { ArrowLeft, Check, MapPin } from 'lucide-react'
import { properties } from '../data/properties'
import { Button, Eyebrow } from '../components/common/UI'
export default function PropertyDetail() { const { slug } = useParams(); const property = properties.find(item => item.slug === slug) || properties[0]; return <main className="detail"><section className="detail-hero"><img src={property.image} alt={property.title}/><div className="detail-shade"/><div className="container detail-copy"><Link to="/properties" className="back"><ArrowLeft size={16}/> All Properties</Link><Eyebrow light>{property.category}</Eyebrow><h1>{property.title}</h1><p><MapPin size={16}/>{property.location}</p><div className="hero-buttons"><Button to="#contact">Book Site Visit</Button><Button to="#overview" kind="outline">Explore details</Button></div></div></section><section id="overview" className="section ivory"><div className="container detail-grid"><div><Eyebrow>PROPERTY OVERVIEW</Eyebrow><h2>Start with a <em>clearer view.</em></h2><p>{property.summary}</p><p>This page introduces an opportunity for conversation. It does not represent confirmed availability, price, approval, title status or investment performance.</p></div><aside className="spec-card"><span>PROPERTY AT A GLANCE</span><div><b>Location</b><p>{property.location}</p></div><div><b>Category</b><p>{property.category}</p></div><div><b>Plot size</b><p>{property.size}</p></div><div><b>Price</b><p>{property.price}</p></div></aside></div></section><section className="section dark"><div className="container"><Eyebrow light>DISCOVER AT YOUR PACE</Eyebrow><h2>Points to discuss<br/>during a <em>site visit.</em></h2><div className="feature-row">{property.features.map(item => <p key={item}><Check size={17}/>{item}</p>)}</div></div></section><section id="contact" className="section contact"><div className="container contact-inner"><Eyebrow light>READY WHEN YOU ARE</Eyebrow><h2>Understand the<br/><em>location first.</em></h2><p>Open a conversation with AVM through the official YouTube or Instagram channel.</p><Button to="https://www.instagram.com/avmproperty/">Connect on Instagram</Button></div></section></main> }
