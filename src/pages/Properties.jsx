import { useMemo, useState } from 'react'
import PropertyCard from '../components/property/PropertyCard'
import { Eyebrow, Reveal } from '../components/common/UI'
import { properties } from '../data/properties'
export default function Properties() { const [type,setType] = useState('All'); const filtered = useMemo(() => type === 'All' ? properties : properties.filter(p => p.category === type), [type]); return <main className="listing-page"><section className="page-hero"><div className="container"><Eyebrow light>AVM OPPORTUNITIES</Eyebrow><h1>Explore <em>properties.</em></h1><p>Selected opportunities designed for an informed, on-ground conversation.</p></div></section><section className="section ivory"><div className="container"><div className="filter-bar"><span>Filter by property type</span>{['All','Residential Plot','Commercial Plot','Investment Property'].map(item => <button className={type === item ? 'selected':''} key={item} onClick={() => setType(item)}>{item}</button>)}</div><div className="property-grid">{filtered.map(item => <Reveal key={item.id}><PropertyCard property={item}/></Reveal>)}</div><p className="listing-disclaimer">The cards are illustrative discovery pages. Availability, documents, approvals, prices and specifications must be independently verified.</p></div></section></main> }
