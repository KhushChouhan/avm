import { ArrowUpRight, Play, X } from 'lucide-react'
import { AnimatePresence, motion } from 'framer-motion'

export function Eyebrow({ children, light = false }) { return <p className={`eyebrow ${light ? 'light' : ''}`}><i />{children}</p> }
export function Button({ children, to = '#contact', kind = 'gold', onClick }) { return <a href={to} onClick={onClick} className={`button ${kind}`}>{children}<ArrowUpRight size={17} /></a> }
export function Reveal({ children, className = '' }) { return <motion.div className={className} initial={{ opacity: 0, y: 22 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .15 }} transition={{ duration: .65, ease: [.2,.7,.2,1] }}>{children}</motion.div> }
export function VideoModal({ video, onClose }) { return <AnimatePresence>{video && <motion.div className="modal-backdrop" onClick={onClose} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.div className="video-modal" onClick={e => e.stopPropagation()} initial={{ opacity: 0, scale: .96, y: 20 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: .96 }}><button className="close" onClick={onClose} aria-label="Close video"><X /></button><Eyebrow>AVM TALKS BY AVNISH</Eyebrow><h2>{video.title}</h2><div className="player"><iframe src={`https://www.youtube-nocookie.com/embed/${video.id}?rel=0`} title={video.title} allow="accelerometer; autoplay; encrypted-media; picture-in-picture" allowFullScreen /></div><a className="plain-link" href={`https://www.youtube.com/watch?v=${video.id}`} target="_blank" rel="noreferrer">Watch directly on YouTube <ArrowUpRight size={16} /></a></motion.div></motion.div>}</AnimatePresence> }
export function PlayMark() { return <span className="play-mark"><Play size={18} fill="currentColor" /></span> }
